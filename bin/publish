#!/usr/bin/env bash

DIR=`dirname $0`
if $DIR/konrad --test; then
    $DIR/commit "$*"
    
    set -e
    
    VERSION=`sds version`
    
    echo 'npm publish' | $DIR/../node_modules/.bin/colorcat -sx
    # npm publish        | $DIR/../node_modules/.bin/colorcat -P $DIR/../cc/npm.noon
fi

