#!/usr/bin/env bash

TEST=test
if [ -e "coffee/test" ] ; then
    TEST="coffee/test/*.coffee"
elif [ -e "test" ] ; then
    TEST="test/*.coffee"
elif [ -e "coffee" ] ; then
    TEST="coffee/test.coffee"
fi

mocha -c --require coffeescript/register --bail "$TEST"
