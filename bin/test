#!/usr/bin/env bash

BIN=`npm bin`

TEST=test
if [ -e "coffee/test" ] ; then
    TEST="coffee/test/*.coffee"
elif [ -e "test" ] ; then
    TEST="test/*.coffee"
fi

$BIN/mocha --require coffeescript/register --bail "$TEST"
