#!/usr/bin/env bash

if [ "`sds -rp 'konrad.bump on'`" = "commit" ]; then
    `dirname $0`/bump
fi

if [ $# -eq 0 ]; then
    MSG="v`sds -rp version`"
else
    MSG="$*"
fi

git add .
git status -sb
git commit -m $MSG

echo 'pushing'

git push -q
git status -sb
